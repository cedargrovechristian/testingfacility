source "https://rubygems.org"

# GitHub Pages gem (matches what GitHub uses to build your site)
gem "github-pages", group: :jekyll_plugins

# Optional: extra plugins GitHub Pages supports
gem "jekyll-seo-tag"
